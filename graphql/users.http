### Query users
GRAPHQL http://localhost:8080/graphql

query {
    users(offset: 10, limit: 10 ) {
        items {
            id
            name
            email
            sex
            __typename
        }
    }
}

### Query by ID
GRAPHQL http://localhost:8080/graphql

query {
    user(id : "10"){
        id
        name
        email
    }
}